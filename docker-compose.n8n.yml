# docker-compose.n8n.yml
services:
  n8n:
    image: n8nio/n8n:latest
    container_name: n8n
    restart: unless-stopped
    ports:
      - "5688:5678"
    environment:
      - N8N_HOST=staging.jobforge.net
      - N8N_PROTOCOL=https
      - N8N_PATH=/n8n/
      - WEBHOOK_URL=https://staging.jobforge.net/n8n/
      - N8N_SECURE_COOKIE=true
      - NODE_ENV=production
      - GENERIC_TIMEZONE=Europe/Riga
      - N8N_ENCRYPTION_KEY=0f3d5a06d9c14e9da0a2e7b3c8f14a2c9c3f7b1e6d9245cbb2a1e8f4d6c79a1f
      - N8N_BASIC_AUTH_ACTIVE=true
      - N8N_BASIC_AUTH_USER=admin
      - N8N_BASIC_AUTH_PASSWORD=S9uC6p!v4rFh3dZkA7qN2wXyB5mQ8tLp
    volumes:
      - n8n_data:/home/node/.n8n
    extra_hosts:
      - "host.docker.internal:host-gateway"

volumes:
  n8n_data:
